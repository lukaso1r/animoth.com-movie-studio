export interface Video {
    id: number
    title: string
    description: string
    thumbnail: string
    videoUrl: string
    wide: boolean
    slug: string
    gridRows: number
    gridCols: number
  }
  